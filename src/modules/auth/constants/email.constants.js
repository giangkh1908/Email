export const SMTP_HOST = process.env.SMTP_HOST || "smtp.gmail.com";
export const SMTP_PORT = process.env.SMTP_PORT || 587;
export const SMTP_SECURE = false;
export const SMTP_FROM = process.env.SMTP_USER;
